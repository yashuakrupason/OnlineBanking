1.When the project is added to IDE,add "mysql-connector-java.jar(download it)" and "servlet-api.jar(from library folder of 
TOMCAT)" files to the project's buildpath.
2.When Customer Registration is done,Customer cannot login immediately .Admin has to change the "is_verified" column 
from 0 to 1 , inorder for customer to Login.
3. Import "OnlineBanking-master" folder to IDE.
4. Import "bank.sql" file to database with bank as Database name.
